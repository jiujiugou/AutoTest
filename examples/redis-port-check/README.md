# Redis 端口探测

## 能帮你做什么

Redis 连不上、没响应、延迟变高——这三种情况任一种发生，钉钉通知你。

## 适用场景

- 生产环境 Redis 宕机检测
- Redis 迁移后验证连通性
- 周期性检查 Redis 响应延迟是否正常

## 你只需要提供

Redis 的地址和端口，默认 `127.0.0.1:6379`

## 怎么用它

任务调度页 → 点「导入」→ 选「Redis 端口探测」→ 填地址端口 → 确定。
