# 定时脚本监控

## 能帮你做什么

你的定时任务（数据同步、报表生成、清理脚本）跑挂了或超时了，钉钉通知你。

## 适用场景

- 夜间数据同步脚本
- 日报/月报生成任务
- 数据库备份脚本
- 日志归档任务

## 你只需要提供

| 参数 | 说明 |
| ---- | ---- |
| 脚本路径 | 服务器上脚本的绝对路径，如 `/opt/scripts/sync.py` |
| 超时时间 | 超过多少秒算失败，默认 300 秒 |

## 怎么用它

任务调度页 → 点「导入」→ 选「定时脚本监控」→ 填路径和超时 → 确定。
