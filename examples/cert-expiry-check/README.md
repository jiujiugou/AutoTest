# SSL 证书过期检查

## 能帮你做什么

HTTPS 证书快过期了（比如还剩不到 30 天），钉钉提前通知你续期。避免证书过期导致用户访问不了网站。

## 适用场景

- 对外网站/API 的 TLS 证书
- 内部服务的自签证书
- 客户交付项目的证书管理

## 你只需要提供

| 参数 | 说明 |
| ---- | ---- |
| 域名 | 要检查的域名，如 `example.com` |
| 告警天数 | 剩余不足多少天时告警，默认 30 天 |

## 怎么用它

任务调度页 → 点「导入」→ 选「SSL 证书过期检查」→ 填域名和告警天数 → 确定。
