# MySQL 端口探测

## 能帮你做什么

MySQL 端口连不上了，钉钉立刻通知你。数据库宕机或网络故障的早期预警。

## 适用场景

- 数据库服务器宕机检测
- 数据库迁移后验证端口可达
- 防火墙/安全组变更后验证

## 你只需要提供

MySQL 的地址和端口，默认 `127.0.0.1:3306`

## 怎么用它

任务调度页 → 点「导入」→ 选「MySQL 端口探测」→ 填地址端口 → 确定。
