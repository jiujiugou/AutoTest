# 外部服务可达性

## 能帮你做什么

检查你的服务器能不能访问外部服务（百度、微信支付、钉钉等）。出网不通或 DNS 故障第一时间发现。

## 适用场景

- 服务器出网连通性巡检
- 第三方 API（微信支付、钉钉回调）可达性验证
- 新服务器上架后的网络验收

## 你只需要提供

服务的地址和端口，如 `www.baidu.com:443`、`api.weixin.qq.com:443`

## 怎么用它

任务调度页 → 点「导入」→ 选「外部服务可达性」→ 填地址端口 → 确定。
